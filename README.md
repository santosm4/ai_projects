# AI Projects

Personal workspace for AI experiments and projects.

## Structure

- `HelloWorld/` — scratch and test scripts
- `Workspace/` — active project directories (each project in its own subdirectory)

## Getting Started

Projects are self-contained. Navigate into any project directory under `Workspace/` for its own setup instructions and tooling.
